# S4 typing-demo — keystroke animation. Shell-prompt fallback (scenarios.md S4):
# the sample prompt is typed at the shell prompt WITHOUT being sent, because
# omp's TUI input box is not reliably drivable headlessly. The substitution is
# recorded in the result `reason`.
Sleep 1
Type@120ms "explain this repo"
Sleep 3
